# GYMRECORD

手機優先的本機健身紀錄 APP。以 Push、Pull、Leg、Upper、Lower 五種分日菜單開始，逐組記錄重量與次數。

## 開始使用

需要 Node.js。於此資料夾執行：

```powershell
npm install
npm run dev
```

在同一台電腦開啟終端顯示的 `http://127.0.0.1:5173/`。正式版本可用 `npm run build` 建立於 `dist/`。

## 目前功能

- 可編輯、新增、刪除訓練菜單與動作。
- 從菜單開始訓練，帶入上次完成的重量與次數。
- 逐組直接輸入或用加減按鈕調整重量、次數，標記暖身組、完成組、增加或刪除組別。
- 訓練中自動儲存；重新開啟後可繼續未完成訓練。
- 訓練時查看每個動作上次完成的正式組重量與次數。
- 上次顯示與新訓練預填共用動作配對規則，會跨菜單採用最近一次已完成的正式組。
- 在日曆查看有訓練的日期，並檢視當天的重量與組數、各動作最高重量趨勢。
- 進步頁可查看跨菜單的逐組動作歷史、重量 PR、各重量的次數 PR 與 Epley 估算 1RM PR。
- 可切換公斤／磅；完成一組後自動倒數休息，可加減 15 秒或跳過。
- 訓練中顯示經過時間與動作備註；完成後顯示時間、組數及正式組訓練量摘要。
- 匯出／匯入 JSON 完整備份，以及匯出 CSV 訓練紀錄。

所有訓練內容儲存在目前瀏覽器的 IndexedDB，沒有帳號與遠端資料庫。清除瀏覽器網站資料或換裝置前，請從設定匯出備份。PWA 的離線快取會在正式建置後啟用；安裝到手機主畫面需要透過支援的安全來源提供 APP。

## Android APK 方向

保留目前的 React 與 Vite 介面，之後以 Capacitor 建立 Android 專案，再透過 Android Studio 建置 APK。手機版仍以本機紀錄為主。瀏覽器與 APK 的儲存空間彼此獨立，換到手機時需先在原裝置匯出 JSON 備份，再於 APK 匯入。封裝前須確認 Android 端的備份匯出／匯入流程與本機資料保存方式。

## 資料結構

菜單含動作與預設值。開始訓練時建立當次快照，保存每一組的實際重量、次數與完成狀態，因此修改菜單不會更動過去紀錄。重量在資料庫中以公斤儲存，顯示時依設定換算。

## 資料存取架構

畫面透過 `src/data/workoutRepository.ts` 的 `WorkoutRepository` 讀寫訓練資料，透過獨立的 `WorkoutChangeSource` 接收資料變更。網頁版由 IndexedDB adapter 使用原本的 `gymrecord` 資料庫與 v1 schema，保留既有紀錄。菜單與 session 可分開查詢；`readAll()` 只供一致性的完整備份使用。還原必須在單一 transaction 中完成，任何失敗都要回滾。同一時間最多一筆進行中的訓練。

備份目前仍輸出 v1 JSON。`src/data/backupSchema.ts` 集中處理版本識別、逐版 migration 與欄位驗證；只有全部完成後才呼叫 `replaceAll()`。未來增加 RPE／RIR 等欄位時，再加入 v1→v2 migration，不直接改寫舊版驗證規則。

之後做 Android APK 時，可在啟動處換成 SQLite adapter，維持同一組資料模型和 `WorkoutRepository` 介面。這只共用資料操作方式，不會讓瀏覽器與手機資料自動同步；離線轉移仍使用 JSON 匯出與匯入。手機版還需處理原生檔案選取與分享，才能完成備份流程。

執行 `npm run build` 檢查型別與建置，`npm test` 執行備份、資料庫及訓練計算測試。

## 手機操作驗證

已用隔離的 Chrome 行動版視窗檢查 320、360、390、412、430 px 寬度，包含 6 組訓練、長動作名稱與備註、休息控制、kg/lb 顯示，以及訓練中重新整理後恢復。另以縮短視窗檢查輸入框聚焦捲動，並模擬背景停用超過休息時間後返回。

**仍需實機驗證：** Android 手機上的實際觸控誤觸、系統數字鍵盤遮擋，以及安裝後的 PWA 模式。這些行為無法由桌面行動版視窗完整代替。
